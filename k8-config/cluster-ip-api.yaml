apiVersion: v1
kind: Service
metadata:
  name: api-cluster-ip
spec:
  type: ClusterIP
  ports:
    # other objects can reach via this port
    - port: 5000
      # this is the exposeed port within the object / container
      targetPort: 5000
  selector:
    component: api
